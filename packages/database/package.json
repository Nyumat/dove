{
    "name": "database",
    "version": "0.0.0",
    "main": "./index.ts",
    "types": "./index.ts",
    "scripts": {
        "db:generate": "prisma generate",
        "db:push": "prisma db push --skip-generate",
        "db:studio": "prisma studio",
        "db:migrate": "prisma migrate dev --name init",
        "clean": "prisma migrate reset --force"
    },
    "dependencies": {
        "@prisma/client": "^5.8.1"
    },
    "devDependencies": {
        "prisma": "latest"
    }
}